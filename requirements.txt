# 마케팅 성과 분석 대시보드 실행에 필요한 패키지
# 설치: pip install -r requirements.txt

# streamlit 상한(<1.50)을 둔 이유:
# 차트/표를 화면 폭에 맞추는 옵션 이름이 최신 버전에서
# use_container_width=True -> width="stretch" 로 변경되었습니다.
# 상한을 풀고 최신 버전을 쓰려면 app.py의 use_container_width=True 를
# width="stretch" 로 바꿔주면 됩니다.
streamlit>=1.30,<1.50  # 웹 대시보드 UI
pandas>=2.0            # 데이터 집계/분석
numpy>=1.24            # 수치 계산 (무한값 처리 등)
plotly>=5.18           # 인터랙티브 차트
pytest>=7.4            # 기본 테스트 실행
